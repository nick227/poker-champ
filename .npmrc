shamefully-hoist=true
# Railway/Nixpacks sets NODE_ENV=production for the build container, which makes pnpm skip
# devDependencies (typescript, prisma CLI, etc). This repo has no multi-stage Docker build --
# the single build phase needs those to compile dist/ before `node dist/...` can run at all.
production=false

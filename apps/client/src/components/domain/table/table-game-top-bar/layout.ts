/**
 * Table game top bar layout contract.
 *
 * Layout values here describe internal typography and spacing.
 * The overall band height (LAYOUT_GAME_TOP_BAR_HEIGHT) is defined in
 * constants/table-layout.constants.ts.
 */

export const TOP_BAR = Object.freeze({
  TABLE_NAME_FONT_SIZE: 16,
} as const);

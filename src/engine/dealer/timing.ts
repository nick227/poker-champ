export const RUNOUT_STAGE_DELAY_MS = 900;
export const HAND_RESULT_HOLD_MS = 2500;
export const NEXT_HAND_DELAY_MS = 300;

// Bot \"thinking\" delay configuration (milliseconds)
export const BOT_ACTION_DELAY_MIN_MS = 500;
export const BOT_ACTION_DELAY_MAX_MS = 3000;

// Legacy constant kept for compatibility; use MIN/MAX for new code.
export const BOT_ACTION_DELAY_MS = 800;

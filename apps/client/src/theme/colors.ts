/** Hex values for RN props (e.g. placeholderTextColor). Aligned with tokens.css for contrast on panel. */
export const PLACEHOLDER_COLOR = "#949494";

/** Modal overlay background (RN style). */
export const BACKDROP_OVERLAY = "rgba(0,0,0,0.6)";

/** Pot win effect: gold ring (RN Animated). */
export const POT_WIN_RING = "rgba(212, 168, 75, 0.9)";

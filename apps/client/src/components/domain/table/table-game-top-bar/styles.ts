import { StyleSheet } from "react-native";
import { TOP_BAR } from "./layout";

export const tableGameTopBarStyles = StyleSheet.create({
  gameTopBarTableName: {
    fontSize: TOP_BAR.TABLE_NAME_FONT_SIZE,
  },
});
